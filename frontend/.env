# Copy to frontend/.env . Base URL of the FastAPI backend.
# Use 127.0.0.1 instead of localhost: uvicorn binds IPv4 only by default, and on
# macOS "localhost" can resolve to IPv6 (::1) first, which breaks the fetch.
VITE_API_BASE_URL=http://127.0.0.1:8000
